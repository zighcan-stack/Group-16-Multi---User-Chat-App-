[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# No external runtime packages needed (tkinter and sockets are built-in)
# Add here if you later use extras like "rich" for colored console output

[dev-packages]
pytest = "*"
pytest-cov = "*"

[requires]
python_version = "3.10"
